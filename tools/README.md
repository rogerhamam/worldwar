# Dev Tools

Standalone tools for working on World War's game data, kept separate from
`game/` (the C++ engine) and `editor/` (the campaign editor) so they can be
run without a build step.

- **`tech_tree_editor/`** — GUI for controlling which techs/units/buildings
  each civilization can access, and for staging proposed unique units. See
  its own README for details.
